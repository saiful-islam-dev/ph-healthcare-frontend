import { Box, Container } from '@mui/material';

const HeroSection = () => {
    return (
        <Container>
            <Box>left</Box>
            <Box>right</Box>
        </Container>
    );
};

export default HeroSection;